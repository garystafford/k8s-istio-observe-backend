apiVersion: v1
kind: Service
metadata:
  name: service-h
  labels:
    name: service-h
    component: service
    version: "1.0.0"
spec:
  ports:
  - name: http
    port: 8000
  selector:
    name: service-h
    component: service
    version: "1.0.0"
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: service-h
  labels:
    name: service-h
    component: service
    version: "1.0.0"
spec:
  replicas: 2
  strategy:
    type: Recreate
  selector:
    matchLabels:
      name: service-h
      component: service
      version: "1.0.0"
  template:
    metadata:
      labels:
        name: service-h
        component: service
        version: "1.0.0"
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      containers:
      - name: service-h
        image: registry.hub.docker.com/garystafford/go-srv-h:1.0.0
        resources:
        # requests:
        #   memory: 100M
        #   cpu: 100m
        # limits:
        #   memory: 250M
        #   cpu: 250m
        env:
        - name: MONGO_CONN
          valueFrom:
            secretKeyRef:
              name: go-srv-config
              key: mongodb.conn
        - name: RABBITMQ_CONN
          valueFrom:
            secretKeyRef:
              name: go-srv-config
              key: rabbitmq.conn
        ports:
        - containerPort: 8000
        imagePullPolicy: IfNotPresent
