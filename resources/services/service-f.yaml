apiVersion: v1
kind: Service
metadata:
  name: service-f
  labels:
    app: service-f
spec:
  ports:
  - name: http
    port: 8000
  selector:
    app: service-f
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: service-f
  labels:
    app: service-f
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: service-f
  template:
    metadata:
      labels:
        app: service-f
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      containers:
      - name: service-f
        image: registry.hub.docker.com/garystafford/go-srv-f:1.0.0
        resources:
        # requests:
        #   memory: 100M
        #   cpu: 100m
        # limits:
        #   memory: 250M
        #   cpu: 250m
        env:
        - name: MONGO_CONN
          valueFrom:
            configMapKeyRef:
              name: go-srv-config
              key: mongodb.conn
        - name: RABBITMQ_CONN
          valueFrom:
            configMapKeyRef:
              name: go-srv-config
              key: rabbitmq.conn
        ports:
        - containerPort: 8000
        imagePullPolicy: IfNotPresent
