apiVersion: v1
kind: Service
metadata:
  name: service-f
  labels:
    app: service-f
    component: service
spec:
  ports:
    - name: http
      port: 8080
  selector:
    app: service-f
    component: service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-f
  labels:
    app: service-f
    component: service
    version: v1
spec:
#  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 1
  selector:
    matchLabels:
      app: service-f
      component: service
      version: v1
  template:
    metadata:
      labels:
        app: service-f
        component: service
        version: v1
    spec:
      containers:
        - name: service-f
          image: {{ .Values.image.repository }}/go-srv-f:{{ .Values.image.tag.ServiceF }}
          resources:
            requests:
              memory: 125Mi
              cpu: 125m
            limits:
              memory: 400Mi
              cpu: 400m
          livenessProbe:
            httpGet:
              path: /api/health
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 3
          env:
            - name: LOG_LEVEL
              value: debug
            - name: MONGO_CONN
              valueFrom:
                secretKeyRef:
                  name: go-srv-config
                  key: mongodb.conn
            - name: RABBITMQ_CONN
              valueFrom:
                secretKeyRef:
                  name: go-srv-config
                  key: rabbitmq.conn
          ports:
            - containerPort: 8080
          imagePullPolicy: {{ .Values.image.pullPolicy }}
